<Window x:Class="HolidayDesktop.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HolidayDesktop"
        mc:Ignorable="d"
        AllowsTransparency="True" 
        WindowStyle="None" 
        Background="{x:Null}" 
        ResizeMode="NoResize"
        IsHitTestVisible="False" 
        Loaded="Window_Loaded" 
        Closing="Window_Closing" 
        ShowInTaskbar="False">
    <Grid>
        <Image Stretch="None" Visibility="{Binding MainImageVisibility}" Source="{Binding ImageAbsoluteFileName}" Panel.ZIndex="1"/>
        <Canvas Name="canvas" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" IsHitTestVisible="False"/>
    </Grid>
</Window>
